#include "contexts/TCP.h"

TCP::TCP()/* : ProtocolContext()*/ {
//	add_context(new ReliabilityContext());
//	add_context(new ConnectionManagerContext());
//	add_context(new CongestionControlContext());
}
